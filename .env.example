# 前后端端口：统一修改 config/ports.json（现场端口冲突时改这一处即可）
#   frontendPort — 前端开发服务器
#   backendPort  — 后端 API

# 前端 API（开发环境通过 Vite 代理到后端，默认 /api）
VITE_API_BASE_URL=/api

# 后端数据库、Dify Key 等见 backend/.env.example
