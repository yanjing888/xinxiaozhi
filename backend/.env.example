# 端口优先读项目根目录 config/ports.json；仅在需要覆盖时设置 PORT
# PORT=8081

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=xinxiaozhi

JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRES_IN=7d

DIFY_BASE_URL=http://188.18.18.149:5001/v1
DIFY_API_KEY=your-dify-api-key-here

# CORS 默认根据 config/ports.json 的 frontendPort 自动生成；多域名部署时可覆盖
# CORS_ORIGIN=http://localhost:5173
